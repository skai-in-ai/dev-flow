#!/usr/bin/env bash
# Explicit local trust expansion only; it never mutates GitHub Issues.
set -euo pipefail
ORCHESTRATOR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
npm --prefix "$ORCHESTRATOR" run --silent build
exec node "$ORCHESTRATOR/dist/repository-onboarding-cli.js" "$@"
