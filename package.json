{
  "name": "agent-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-agnostic, approval-friendly coding-agent review workflow.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.extensions.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "orchestrate": "npm run build && node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
